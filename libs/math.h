#ifndef MATH_H
#define MATH_H

//取整
#define ceil(x) (int)((x) == (int)(x) ? (x) : (x + 1))
#define floor(x) (int)(x)

#define max(a,b) ((a)>(b)?(a):(b))
#define min(a,b) ((a)<(b)?(a):(b))

#endif  //METH_H